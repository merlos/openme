
cli/openme
cli/openme-*
cli/dist/
cli/coverage.out

# ── Quarto docs ─────────────────────────────────────────────
docs/_site/
docs/.quarto/

# ── macOS ───────────────────────────────────────────────────
.DS_Store
**/.DS_Store

# ── Windows ─────────────────────────────────────────────────
Thumbs.db

# ── IDEs ────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── Xcode (macOS / iOS / watchOS / tvOS) ────────────────────
# Legacy paths (pre-apple/ restructure)
macos/*.xcworkspace/xcuserdata/
ios/*.xcworkspace/xcuserdata/
macos/DerivedData/
ios/DerivedData/

# User-specific Xcode state (applies to the whole apple/ tree)
apple/**/*.xcworkspace/xcuserdata/
apple/**/*.xcodeproj/xcuserdata/
apple/**/*.xcworkspace/xcshareddata/swiftpm/resolution.json
apple/**/xcuserdata/
**/*.xcuserdata

# Derived data & build artefacts
apple/DerivedData/
DerivedData/

# Swift Package Manager
apple/**/.build/
apple/**/Package.resolved
.build/

# Playgrounds
apple/**/*.playground/timeline.xctimeline
apple/**/*.playground/playground.xcworkspace

# Crashlytics / Fastlane
apple/**/Crashlytics/
apple/**/fastlane/report.xml
apple/**/fastlane/Preview.html
apple/**/fastlane/screenshots/
apple/**/fastlane/test_output/

# CocoaPods (not used, but prevent accidental commits if ever added)
apple/**/Pods/
apple/**/Podfile.lock

# Xcode Previews
apple/**/__Previews/

# TestFlight / Archives
apple/**/*.xcarchive
apple/**/*.ipa
apple/**/*.dSYM.zip
apple/**/*.dSYM

# ── Android ─────────────────────────────────────────────────
# Gradle build outputs & caches
android/.gradle/
android/**/build/
android/**/.gradle/

# Local config — machine-specific, never commit
android/local.properties

# Android Studio / IntelliJ project files (covered globally by .idea/ above,
# but keep explicit paths for safety)
android/.idea/
android/**/*.iml

# Compiled outputs & release artefacts
*.apk
*.aab
*.ap_
android/captures/

# Signing keystores — NEVER commit key material
*.jks
*.keystore
android/keystore/

# ProGuard / R8 mapping (generated per build)
android/**/mapping.txt
android/**/missing_rules.txt

# Navigation / lint generated files
android/**/lint/
android/**/lint-results*
android/**/lint_*.xml

# Gradle wrapper JAR (download-on-demand; not needed in VCS)
android/gradle/wrapper/gradle-wrapper.jar

# Flavour / test coverage reports
android/**/reports/

# ── openme runtime — NEVER commit keys ──────────────────────
*.key
/etc/openme/
cli/*.yaml
